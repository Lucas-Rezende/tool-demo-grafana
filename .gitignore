# Dataset congelado. São centenas de MB de blocos de TSDB, chunks de log e
# blocos de trace — não entra no Git. Combine com o grupo como compartilhar
# (pendrive, Drive, zip no Classroom). Ver docs/04-demo-reprodutivel.md.
snapshot/

# Clone do repositório oficial da Grafana, criado por 'make setup'.
# É código de terceiros e é recriável a partir do commit fixado em
# stack/_comum.sh; não faz sentido versionar aqui.
intro-to-mltp/

# Metadados gerados por 'make setup' na máquina local.
stack/.setup-info

# Lixo de sistema operacional e de editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*~
